export type UserForm = {
  name: string;
  email: string;
  password: string;
  surname: string;
  secondName: string;
  phone: string;
};

export type WorkspaceForm = {
  name: string;
  description?: string;
};
