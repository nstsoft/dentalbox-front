export enum TAG {
  USER = "USER",
  AUTH = "AUTH",
  LOGIN = "LOGIN",
  PLAN = "PLAN",
  WORKSPACE = "WORKSPACE",
}

export enum REDUCER {
  USER = "USER",
  AUTH = "AUTH",
  PLAN = "PLAN",
  WORKSPACE = "WORKSPACE",
}

export enum CACHE_KEYS {
  LOGIN = "LOGIN",
}
