export enum TAG {
  USER = "USER",
  AUTH = "AUTH",
  LOGIN = "LOGIN",
  PLAN = "PLAN",
}

export enum REDUCER {
  USER = "USER",
  AUTH = "AUTH",
  PLAN = "PLAN",
}

export enum CACHE_KEYS {
  LOGIN = "LOGIN",
}
