export enum TAG {
  USER = "USER",
  AUTH = "AUTH",
  LOGIN = "LOGIN",
  PRODUCT = "PRODUCT",
  WORKSPACE = "WORKSPACE",
}

export enum REDUCER {
  USER = "USER",
  AUTH = "AUTH",
  PRODUCT = "PRODUCT",
  WORKSPACE = "WORKSPACE",
}

export enum CACHE_KEYS {
  LOGIN = "LOGIN",
}
